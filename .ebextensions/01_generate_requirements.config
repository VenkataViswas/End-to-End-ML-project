commands:
  01_generate_requirements:
    command: |
      cd /var/app/staging
      poetry export -f requirements.txt --output requirements.txt
  02_install_requirements:
    command: |
      source /var/app/venv/*/bin/activate
      pip install -r /var/app/staging/requirements.txt
